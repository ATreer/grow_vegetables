<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hz.mapper.PackageMapper">
    <select id="selectAllByUserId" resultType="com.hz.domain.complex.PackageObj" parameterType="int">
        SELECT p.id,p.grower_id,p.max_package_count,p.obj_count,p.obj_id,p.obj_type,obj.grade,obj.`name`,obj.`desc`,obj.type,obj.image_url
        from package p JOIN  `game_obj` obj on obj.id = p.obj_id  WHERE  p.grower_id = #{id};
    </select>
</mapper>